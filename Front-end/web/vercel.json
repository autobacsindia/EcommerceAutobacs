{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "next build",
  "regions": ["bom1"],
  "ignoreCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then exit 1; else git diff --quiet HEAD^ HEAD -- .; fi",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "develop": true
    }
  }
}
